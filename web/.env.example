# PostgreSQL connection for the Next.js API routes (same DB where sql/create_db.sql was applied).
# Example when using README Docker (database name may be spotify_streaming_analytics or postgres depending on your setup):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
